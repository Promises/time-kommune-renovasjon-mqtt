[Unit]
Description=Renovasjon MQTT Bridge Timer
Requires=renovasjon-mqtt.service

[Timer]
# Run every hour
OnCalendar=hourly
# Run 5 minutes after boot if we missed a scheduled run
OnBootSec=5min
# Ensure it runs even if the last run was missed
Persistent=true

[Install]
WantedBy=timers.target
